# docs/ — design material

`paper2video_method.png` is the pipeline diagram shown on the site under "How a video
gets made". Keep the editable source (the draw.io file) here too when exporting a new
version, and export at the same 1565 x 938 size so the page layout does not shift.
