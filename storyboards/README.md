# storyboards/ — one JSON per paper or lecture

Name each file after the video it produces, so `storyboards/lejepa.json` renders
`videos/lejepa.mp4` and pairs with `posters/lejepa.jpg` and `captions/lejepa.vtt`.
Storyboards without a rendered video are fine here too.
